<div class="text-center">
  <h2>{{post title}}</h2>
  <p>{{post.content}}</p>
  <p>Created by {{post.user.name}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Create Post</button>
  </p>
</div>
